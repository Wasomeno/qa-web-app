{
  "id": "fd33e16a",
  "title": "Add branch selector to specs page",
  "tags": [],
  "status": "done",
  "created_at": "2026-04-29T07:46:16.221Z"
}

## Plan

### Backend (already done)
- ✅ `GET /projects/:id/branches` endpoint exists
- ✅ Specs endpoints accept `ref` param for branch

### Frontend changes needed

1. **Create `useProjectBranches` hook** - fetch branches for selected project
2. **Add branch selector to specs-page.tsx** - dropdown in the header bar
3. **Wire branch (`ref`) through all queries** - tree, file, commits all need the selected branch
4. **Wire branch through mutations** - save, delete, commit need the branch
5. **Persist branch selection** in localStorage per project
